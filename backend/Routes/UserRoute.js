const express = require("express");
const userControllers = require("./../Controllers/userControllers");

const Router = express.Router();

// Router.route("/:id").get(userControllers.getRecommondations);

module.exports = Router;